"""MCP tools — one module per category (state, htb, vpn, recon, intel, vuln, creds, exploit, post, ad, session, flag, writeup, narrate, phase).

All modules import the central registry from kestrel.mcp.registry and register their tools via the @tool decorator.
"""
