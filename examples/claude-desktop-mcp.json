{
  "_comment": "Kestrel MCP server config for Claude Desktop. Add this block to %APPDATA%/Claude/claude_desktop_config.json (Windows) under the top-level mcpServers key, then restart Claude Desktop.",
  "mcpServers": {
    "kestrel": {
      "command": "C:/opsec/runner/.venv/Scripts/kestrel-mcp.exe",
      "args": [
        "--state-dir",
        "C:/Proyectos/CobaltoSec/fleet/agents/htb/state",
        "--session-root",
        "C:/Proyectos/CobaltoSec/sectors/red-team/htb-sessions",
        "--log-level",
        "INFO"
      ]
    }
  }
}
