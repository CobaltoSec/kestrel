# Kestrel MCP — Tools Reference (v0.4.0-dev)

_Autogenerated 2026-05-20T01:10:15Z by `scripts/gen_tools_reference.py`._

**Totals:** 70 tools · 10 prompts · 10 resources

## Index

- [ad](#ad) (4 tools)
- [creds](#creds) (6 tools)
- [exploit](#exploit) (6 tools)
- [flag](#flag) (2 tools)
- [heartbeat](#heartbeat) (2 tools)
- [hitl](#hitl) (1 tool)
- [htb](#htb) (6 tools)
- [intel](#intel) (4 tools)
- [kali](#kali) (2 tools)
- [meta](#meta) (2 tools)
- [narrate](#narrate) (1 tool)
- [phase](#phase) (2 tools)
- [post](#post) (8 tools)
- [recon](#recon) (6 tools)
- [session](#session) (4 tools)
- [state](#state) (4 tools)
- [vpn](#vpn) (3 tools)
- [vuln](#vuln) (4 tools)
- [writeup](#writeup) (3 tools)

---

## ad

### `ad_asreproast`

impacket-GetNPUsers — query AS-REP roastable accounts (UF_DONT_REQUIRE_PREAUTH). Returns hashes ($krb5asrep$, hashcat -m 18200).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `domain` | `string` | ✓ | `—` |
| `dc` | `string` | ✓ | `—` |
| `users_file` | `string` |   | `—` |
| `user` | `string` |   | `—` |

### `ad_bloodhound_collect`

Run bloodhound-python collector via Kali. Outputs zip with computer/user/group JSON. Requires creds (user/password) and a DC reachable from Kali.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `domain` | `string` | ✓ | `—` |
| `user` | `string` | ✓ | `—` |
| `password` | `string` | ✓ | `—` |
| `dc` | `string` | ✓ | `—` |
| `collection` | `string` |   | `All` |

### `ad_dcsync`

impacket-secretsdump full DCSync (NTDS) — requires a DA-equivalent account. Returns the user:rid:lmhash:nthash:::-formatted list.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `domain` | `string` | ✓ | `—` |
| `user` | `string` | ✓ | `—` |
| `password_or_hash` | `string` | ✓ | `—` |
| `dc` | `string` | ✓ | `—` |

### `ad_kerberoast`

impacket-GetUserSPNs to dump Kerberoastable service tickets. Returns the TGS hashes as a list of strings. Hashes format $krb5tgs$ are hashcat -m 13100.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `domain` | `string` | ✓ | `—` |
| `user` | `string` | ✓ | `—` |
| `password` | `string` | ✓ | `—` |
| `dc` | `string` | ✓ | `—` |

---

## creds

### `creds_default_check`

Try a built-in dict of default credentials for the given service (ssh/mysql/ftp/rdp/tomcat/etc.). Uses nxc (NetExec). Returns success on first hit.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `service` | `string` | ✓ | `—` |
| `target` | `string` | ✓ | `—` |
| `port` | `string` |   | `—` |

### `creds_hash_crack`

Crack a single hash via hashcat on Kali. Returns rc + cracked password if found. For bcrypt/argon2/scrypt prefer async pipeline (use crack-helper.sh from skill flow).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `hash_value` | `string` | ✓ | `—` |
| `hash_type` | `string` | ✓ | `—` |
| `wordlist_path` | `string` | ✓ | `—` |
| `rules` | `string` |   | `none` |
| `timeout` | `string` |   | `300` |

### `creds_hash_recommend`

Recommend a ranked wordlist strategy for a hash given context (machine_name, vhosts, framework). Wraps kestrel.core.wordlist.build_plan.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `hash_type` | `string` | ✓ | `—` |
| `machine_name` | `string` | ✓ | `—` |
| `vhosts` | `array` |   | `—` |
| `framework` | `string` |   | `—` |
| `target_ip` | `string` |   | `—` |

### `creds_hash_status`

Check the status of an async hash crack job (created by crack-helper.sh --async). Returns pending_upload / running / complete / no_match / timeout / error.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `job_id` | `string` | ✓ | `—` |
| `jobs_dir` | `string` |   | `—` |

### `creds_password_spray`

Password spray a single password across a user list against `target`. Service auto-detected from protocol arg (smb/winrm/ssh/mssql). Use small lists — 1 password per call to avoid AD lockout.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `users` | `string` | ✓ | `—` |
| `password` | `string` | ✓ | `—` |
| `protocol` | `string` |   | `smb` |

### `creds_save_tried`

Append a tried credential / endpoint / hash to state.machines[machine] cross-session arrays. kind must be 'credential', 'endpoint', or 'hash'. Auto-stamps ts if missing.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `kind` | `string` | ✓ | `—` |
| `payload` | `object` | ✓ | `—` |

---

## exploit

### `exploit_run_msf`

Execute a Metasploit module via RPC. Returns job_id + session_id if a session is established within `wait_timeout`. Returns available=False if msfrpcd is down.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `module` | `string` | ✓ | `—` |
| `options` | `object` | ✓ | `—` |
| `payload` | `string` |   | `—` |
| `wait_timeout` | `number` |   | `30.0` |

### `exploit_run_poc`

Run an external PoC script on Kali via shell. `script` is an absolute path on Kali; `args` are appended. Use for one-off CVE-specific scripts not in MSF.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `script` | `string` | ✓ | `—` |
| `args` | `string` |   | `—` |
| `timeout` | `string` |   | `300` |

### `exploit_smb_psexec`

impacket-psexec foothold. `password_or_hash` may be plaintext or NT hash (32 hex chars). Uses pass-the-hash automatically when input matches NT hash pattern.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `user` | `string` | ✓ | `—` |
| `password_or_hash` | `string` | ✓ | `—` |
| `domain` | `string` |   | `.` |

### `exploit_web_lfi`

Probe a local file inclusion vulnerability. Replaces `{}` in url with the encoded path traversal payload. Returns the response body (truncated) so the LLM can confirm file contents.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `url_template` | `string` | ✓ | `—` |
| `target_file` | `string` |   | `/etc/passwd` |
| `traversals` | `string` |   | `6` |

### `exploit_web_rce`

Send an RCE payload via HTTP. `url_template` should include `{cmd}` placeholder. Returns response excerpt. technique is informational only ('shellshock', 'log4j', 'php-eval', etc.).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `url_template` | `string` | ✓ | `—` |
| `command` | `string` |   | `id` |
| `technique` | `string` |   | `generic` |

### `exploit_winrm`

Connect via evil-winrm (or pass-the-hash). Runs `cmd` and returns output.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `user` | `string` | ✓ | `—` |
| `password_or_hash` | `string` | ✓ | `—` |
| `cmd` | `string` |   | `whoami` |

---

## flag

### `flag_extract`

Extract user.txt + root.txt (Linux) or user.txt + admin.txt (Windows) via the given exec template. Returns dict {user_flag, root_flag, raw_outputs}. Pass exec_cmd_template with `{}` placeholder for the inner cmd.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `exec_cmd_template` | `string` | ✓ | `—` |
| `os` | `string` |   | `linux` |

### `flag_validate`

Validate an HTB flag format (32 hex characters). Returns {valid: bool, normalized: str|None, reason?}.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `flag` | `string` | ✓ | `—` |

---

## heartbeat

### `heartbeat_status`

Compute the full session dashboard: elapsed_min, budget_min, idle_min, top_time_sinks, suggestion. Wraps core.heartbeat.emit_dashboard_data. Returns the dashboard dict.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |

### `stuck_check`

Run stuck signal detection over a machine's session_dir. Returns ranked signals + recommendation + alternatives. Recommendations: continue / switch_vpn_server / reset_listener / escalate_gpu / switch_vector.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |

---

## hitl

### `request_user_confirmation`

Request explicit operator confirmation via a HITL marker. The MCP client (Claude Code) should display the question + options and wait for the operator's answer. Use for critical gates: machine pick, vector confirm, exploit confirm, submit flag, debrief.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `question` | `string` | ✓ | `—` |
| `options` | `array` |   | `—` |
| `context` | `string` |   | `—` |
| `default_option` | `string` |   | `—` |

---

## htb

### `htb_list_machines`

List HTB machines, optionally filtered. status='retired' (default) or 'active'. Returns list of {id, name, os, difficultyText, ip, ...}.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `status` | `string` |   | `retired` |
| `difficulty` | `string` |   | `—` |
| `os` | `string` |   | `—` |

### `htb_machine_info`

Get full machine info by slug (name) or ID. Returns dict with id, name, os, difficulty, ip, points, tags.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `slug` | `string` | ✓ | `—` |

### `htb_profile_update`

Fetch the current HTB profile (rank, points, owns) and persist to <state_dir>/profile.json. Returns the fetched profile dict.

### `htb_release`

Release (terminate) an HTB machine. Resolves slug→id automatically.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `slug` | `string` | ✓ | `—` |

### `htb_spawn`

Spawn an HTB machine on the free VPN. Resolves slug→id automatically. Persists machine_id, machine_os, machine_difficulty, target_ip into state.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `slug` | `string` | ✓ | `—` |

### `htb_submit_flag`

Submit a user or root flag for an HTB machine. flag_type is 'user' or 'root' (informational). difficulty 10-100 (self-reported).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `slug` | `string` | ✓ | `—` |
| `flag` | `string` | ✓ | `—` |
| `flag_type` | `string` |   | `user` |
| `difficulty` | `string` |   | `50` |

---

## intel

### `intel_classify_blind`

Classify a target from ports/services/banners into ranked attack categories + attack plan. Uses kestrel.core.fingerprint. KB query auto-runs if KESTREL_KB_PATH is set.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `ports` | `array` |   | `—` |
| `services` | `array` |   | `—` |
| `banners` | `array` |   | `—` |
| `os_hint` | `string` |   | `` |
| `framework` | `string` |   | `—` |
| `ad_joined` | `boolean` |   | `False` |

### `intel_cve_lookup`

Ranked CVE pipeline for product+version: 1) KB synthesis chunks 2) NVD API 3) ExploitDB local CSV 4) MSF search RPC. Each backend independent — failures don't block.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `product` | `string` | ✓ | `—` |
| `version` | `string` | ✓ | `—` |

### `intel_kb_query`

Query the pgvector KB with `query` (free text). Returns top_k chunks with score + source. Falls back to empty list if KB unavailable. timeout 5s hard cap.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | ✓ | `—` |
| `top_k` | `string` |   | `5` |

### `intel_save_synthesis`

Persist an intel.md synthesis to <session_dir>/intel.md and update state.machines[machine] with intel_path + intel_confidence + intel_sources.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `content_md` | `string` | ✓ | `—` |
| `sources` | `array` |   | `—` |
| `confidence` | `string` | ✓ | `—` |

---

## kali

### `kali_ping_target`

Send 3 ICMP echo requests from Kali to target_ip. Returns rc 0 if reachable. Useful right after vpn_up to confirm HTB target is up.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target_ip` | `string` | ✓ | `—` |

### `kali_status`

Check Kali VM SSH reachability + return hostname/uname/whoami/uptime. Use before running any other tool that requires Kali.

---

## meta

### `kestrel_ping`

Health check — echoes a message with a timestamp. Used for MCP handshake validation.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `message` | `string` |   | `ping` |

### `kestrel_version`

Return Kestrel version + loaded tool/prompt/resource counts.

---

## narrate

### `narrate_emit`

Emit a narration line (📡 discover / 🔍 analyze / 💡 decide / ➡ advance) to <session_dir>/estado.md AND <session_dir>/sessions.jsonl. machine defaults to state.current_session if omitted.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `stream` | `string` | ✓ | `—` |
| `text` | `string` | ✓ | `—` |
| `machine` | `string` |   | `—` |

---

## phase

### `phase_current`

Get current phase + active session + machines that haven't been owned or abandoned.

### `phase_enter`

Enter a phase (p0_setup, p1_recon, p2_vector, p3_exploit, p4_privesc, p5_close). Persists state.current_phase and returns the phase guidance (description, suggested tools, HITL gates).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `phase` | `string` | ✓ | `—` |

---

## post

### `post_check_token`

Parse Windows `whoami /priv` output, flag exploitable privileges (SeImpersonate, SeAssign, etc.) and suggest Potato/PrintNightmare/etc.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `whoami_priv_output` | `string` | ✓ | `—` |

### `post_enum_system`

Quick system enum: uname/lsb_release/uptime (Linux) or systeminfo (Windows).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `exec_cmd_template` | `string` | ✓ | `—` |
| `os` | `string` |   | `linux` |

### `post_enum_user`

Quick user enum on target: id; whoami; groups; sudo -l (or whoami /priv on Windows).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `exec_cmd_template` | `string` | ✓ | `—` |
| `os` | `string` |   | `linux` |

### `post_linpeas_run`

Download + run linpeas.sh on target (via session_exec proxy command). Returns top findings only. `exec_cmd_template` should contain {} where the command goes (e.g. SSH user@host 'bash -c ...').

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `exec_cmd_template` | `string` | ✓ | `—` |
| `linpeas_url` | `string` |   | `https://github.com/peass-ng/PEASS-ng/releases/latest/download/linpeas.sh` |

### `post_privesc_kernel`

Match `kernel_version` against known kernel exploits (Dirty COW, OverlayFS, PwnKit, etc.).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `kernel_version` | `string` | ✓ | `—` |

### `post_privesc_potato`

Suggest the right Potato variant based on Windows build + privilege. variant is informational; this tool returns a copy-paste exec line (the actual binary deploy is up to the LLM/user).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `windows_build` | `string` | ✓ | `—` |
| `variant` | `string` |   | `auto` |

### `post_privesc_sudo`

Parse the output of `sudo -l` and match binaries against the gtfobins escape catalog. Returns matches with copy-paste-ready PoC commands.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `sudo_l_output` | `string` | ✓ | `—` |

### `post_winpeas_run`

Same as post_linpeas_run but for WinPEAS (Windows). Output captured + heuristic-filtered.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `exec_cmd_template` | `string` | ✓ | `—` |
| `winpeas_url` | `string` |   | `https://github.com/peass-ng/PEASS-ng/releases/latest/download/winPEASx64.exe` |

---

## recon

### `recon_dns_enum`

DNS enumeration via dig + dnsenum (NS, MX, SOA, axfr attempt) against target.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `domain` | `string` |   | `—` |
| `machine` | `string` |   | `—` |

### `recon_ldap_enum`

LDAP anonymous bind + naming context enumeration via ldapsearch.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `machine` | `string` |   | `—` |

### `recon_nmap_scan`

Run nmap against `target` with the given profile (quick|full|udp|nse_smb). Returns parsed ports + services. Saves raw XML to <session_dir>/recon/nmap/ if machine given.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `profile` | `string` |   | `quick` |
| `ports` | `string` |   | `—` |
| `machine` | `string` |   | `—` |

### `recon_service_probe`

Probe a single target:port with nmap version detection + scripts (-sV -sC --version-all). Use for service-specific deep-dive after recon_nmap_scan.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `port` | `string` | ✓ | `—` |
| `service_hint` | `string` |   | `—` |
| `machine` | `string` |   | `—` |

### `recon_smb_enum`

Enumerate SMB shares + users via smbclient + enum4linux-ng on Kali.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `machine` | `string` |   | `—` |

### `recon_web_fingerprint`

HTTP(S) fingerprint via curl: status code, server, headers, title. Use on web ports identified by nmap (80, 443, 8080, etc.).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `port` | `string` |   | `80` |
| `machine` | `string` |   | `—` |

---

## session

### `session_close`

Close session `handle_id` and remove from the registry.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `handle_id` | `string` | ✓ | `—` |

### `session_exec`

Execute `cmd` on the session identified by `handle_id`. Returns stdout/stderr/rc/duration.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `handle_id` | `string` | ✓ | `—` |
| `cmd` | `string` | ✓ | `—` |
| `timeout` | `string` |   | `120.0` |

### `session_list`

List all active session handles with their type. Returns [{handle_id, type}].

### `session_open`

Open a transport session (ssh|winrm|msf) and register it. Returns handle_id for subsequent session_exec calls. params shape depends on transport: ssh={host,user,key_path|password,port?}, msf={} (uses RPC config), winrm={host,user,password|cert}.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `transport` | `string` | ✓ | `—` |
| `params` | `object` | ✓ | `—` |

---

## state

### `state_append_event`

Append an audit event to <session_dir>/sessions.jsonl. session_dir is resolved from the given `machine` (via its session_slug) or, if absent, from state.current_session.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `phase` | `string` | ✓ | `—` |
| `event` | `string` | ✓ | `—` |
| `detail` | `string` |   | `—` |
| `machine` | `string` |   | `—` |

### `state_read`

Read current Kestrel state. If `machine` is given, returns just that machine's slice; otherwise returns a top-level summary (run_count, phase, machine list).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` |   | `—` |

### `state_session_dir`

Resolve the session directory for a machine and ensure it exists. Returns the absolute path.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |

### `state_write_machine`

Patch a machine's state by shallow-merging `patch` into machines[machine]. Creates the entry if absent. Returns the merged state.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `patch` | `object` | ✓ | `—` |

---

## vpn

### `vpn_down`

Tear down the HTB OpenVPN connection on Kali.

### `vpn_status`

Check HTB OpenVPN status on Kali (interface state, IP, peer).

### `vpn_up`

Bring up the HTB OpenVPN connection on Kali. Optional `server` arg picks a region (e.g. 'eu-vip-1'). Returns rc + stdout/stderr from htb-vpn.sh.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `server` | `string` |   | `—` |

---

## vuln

### `vuln_check_exploit_db`

Search the local ExploitDB CSV mirror for `query` (case-insensitive). Defaults to ~/.kestrel/exploitdb.csv (override via env KESTREL_EXPLOITDB_CSV).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | ✓ | `—` |
| `limit` | `string` |   | `10` |

### `vuln_msf_search`

Search Metasploit modules matching `query` via RPC. Returns list of modules with rank/type. Returns available=False with reason if msfrpcd is unreachable.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | ✓ | `—` |

### `vuln_nuclei_broad`

Wide nuclei scan filtered by severity (critical/high/medium/low). Slower — use after vuln_nuclei_targeted when ad-hoc CVE lookup turned up nothing.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `severity` | `string` |   | `critical,high` |
| `machine` | `string` |   | `—` |

### `vuln_nuclei_targeted`

Run nuclei with specific templates against `target`. `templates` is a list of template IDs, tags, or paths (e.g. ['CVE-2007-2447', 'samba']). Returns parsed findings.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target` | `string` | ✓ | `—` |
| `templates` | `array` | ✓ | `—` |
| `severity` | `string` |   | `—` |
| `machine` | `string` |   | `—` |

---

## writeup

### `writeup_generate`

Render a Markdown write-up for `machine` from `sections` + `flags`. Saves to <session_dir>/writeup.md. sections keys: summary, recon, foothold, privesc, lessons (all optional).

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `sections` | `object` |   | `—` |
| `flags` | `object` |   | `—` |
| `os` | `string` |   | `—` |
| `difficulty` | `string` |   | `—` |
| `ip` | `string` |   | `—` |

### `writeup_kb_synthesize`

Synthesize a KB-ingestable markdown from a finished machine's write-up. Strips spoilers (commands), keeps techniques + CVE refs. Saves to kb/staging/htb-<machine>.md.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `kb_staging_dir` | `string` |   | `—` |

### `writeup_publish_hint`

Emit a publish-hint JSON via the shared publish-prep/emit.py helper. Tells downstream JobSearch automation that a new write-up is ready.

**Parameters:**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `machine` | `string` | ✓ | `—` |
| `content_md_path` | `string` |   | `—` |

---

## Prompts

### `debrief_template`

5-section feedback.md template — HARD GATE in p5_close before cleanup.

### `hint_generation`

Generate a 1-line anti-spoiler hint for /kestrel hint. Provides phase + last narration as context.

### `intel_synthesis_template`

Template + anti-spoiler rules + confidence rating for writing machine intel.md.

### `kestrel_kickoff`

Initial Kestrel orchestrator role + phase + narration + HITL rules. Call once at /kestrel start.

### `p0_setup`

Phase p0_setup — machine pick + intel + spawn + ping.

### `p1_recon`

Phase p1_recon — nmap + service enum + classify.

### `p2_vector`

Phase p2_vector — propose ranked vectors + HITL confirm.

### `p3_exploit`

Phase p3_exploit — run confirmed vector, open session.

### `p4_privesc`

Phase p4_privesc — enum + escalate to root/SYSTEM.

### `p5_close`

Phase p5_close — flags + submit + writeup + cleanup + debrief.

---

## Resources

| URI | Name | Description |
|-----|------|-------------|
| `kestrel://config` | config | Active server configuration (state dir, session root, version). |
| `kestrel://kb/categories` | kb-categories | Attack category catalog used by intel_classify_blind (category, description, tactics, kb_tags). |
| `kestrel://session/{machine}/findings` | session-findings | Read <session_dir>/findings.md (live findings notebook). |
| `kestrel://session/{machine}/fingerprint` | session-fingerprint | Read <session_dir>/fingerprint.json (blind_fingerprint output). |
| `kestrel://session/{machine}/intel` | session-intel | Read <session_dir>/intel.md for the given machine slug. |
| `kestrel://session/{machine}/recon` | session-recon | List files under <session_dir>/recon/ (nmap XMLs, web fingerprints, smb dumps). |
| `kestrel://session/{machine}/writeup` | session-writeup | Read <session_dir>/writeup.md (generated by writeup_generate). |
| `kestrel://state/last-cycle` | last-cycle | Top-level Kestrel state (LastCycle JSON — run_count, machines, current_phase, etc.). |
| `kestrel://state/profile` | profile | HTB user profile snapshot (from state_dir/profile.json). |
| `kestrel://state/sessions-jsonl` | sessions-jsonl | Last 100 entries from current session's sessions.jsonl (audit log). |
